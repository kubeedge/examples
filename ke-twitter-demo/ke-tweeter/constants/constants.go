package constants

const (
	// MergePatchType is patch type
	MergePatchType = "application/merge-patch+json"
	// ResourceTypeDevices is plural of device resource in apiserver
	ResourceTypeDevices = "devices"
)

// Twitter OAuth Credentials Constants
const (
	TwitterAccessToken       = "ACCESS_TOKEN"
	TwitterAccessTokenSecret = "ACCESS_TOKEN_SECRET"
	TwitterConsumerKey       = "CONSUMER_KEY"
	TwitterConsumerSecret    = "CONSUMER_SECRET"
)
