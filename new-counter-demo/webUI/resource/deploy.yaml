apiVersion: apps/v1
kind: Deployment
metadata:
  name: counter-webui
  labels:
    app: counter-webui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: counter-webui
  template:
    metadata:
      labels:
        app: counter-webui
    spec:
      hostNetwork: true
      nodeName: kind-control-plane # 1. replace the nodeName with your edge node name
      containers:
        - name: counter-webui
          image: counter-webui:v1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
