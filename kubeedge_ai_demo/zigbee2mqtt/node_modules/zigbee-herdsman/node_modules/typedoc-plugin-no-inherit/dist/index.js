"use strict";
const plugin_1 = require("./plugin");
module.exports = (PluginHost) => {
    const app = PluginHost.owner;
    app.converter.addComponent('no-inherit', new plugin_1.NoInheritPlugin(app.converter));
};
